role:
  statements:
    - ${file(serverless/aws/dynamodb.yml):iamRoleStatements}
    - ${file(serverless/aws/event-bus.yml):iamRoleStatements}
    - ${file(serverless/aws/kms.yml):iamRoleStatements}
    - ${file(serverless/aws/cloudwatch.yml):iamRoleStatements}
    - ${file(serverless/aws/firehose.yml):iamRoleStatements}
    - ${file(serverless/aws/lambda.yml):iamRoleStatements}
    - ${file(serverless/aws/secrets.yml):iamRoleStatements}